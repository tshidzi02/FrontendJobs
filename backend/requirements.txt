
# =============================================================================
# FILE: backend/requirements.txt
# =============================================================================
# PURPOSE: Lists every Python package this project needs.
#          Anyone can reproduce your exact environment with:
#            pip install -r requirements.txt
#
# Install all at once with:
#   cd backend
#   pip install -r requirements.txt
# =============================================================================

flask
flask-cors
flask-bcrypt
flask-jwt-extended
flask-sqlalchemy
psycopg2-binary
gunicorn
openai
numpy
google-auth
google-auth-oauthlib
docxtpl
python-dotenv
